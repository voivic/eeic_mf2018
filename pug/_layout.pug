doctype html
html(lang="ja")
	head
		meta(charset="utf-8")
		meta(name="viewport" content="width=device-width,initial-scale=1.0,minimum-scale=1.0")
		block title
		link(rel="icon" href="img/black_bulb.png")
		link(rel="stylesheet" href="css/reset.css")
		link(rel="stylesheet" href="css/header.css")
		link(rel="stylesheet" href="css/common.css")
		link(rel="stylesheet" href="css/jquery.bxslider.min.css")
		script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js")
		script.
			$(function() {
				$('#navToggle').click(function(){//headerに .openNav を付加・削除
					$('header').toggleClass('openNav');
				});
				$('main, .header-global-nav-list').click(function(){
				if($('header').hasClass('openNav')){
					$('header').removeClass('openNav');
				}
				})
			})

			$(function(){
				$('.lab-img').click(function(){
					$(this).nextAll(".lab-text").toggleClass('lab-opentext');
				})
			})
			
		script.
				$(function() {
				// スクロールのオフセット値
				var offsetY = -20;
				// スクロールにかかる時間
				var time = 500;
				// ページ内リンクのみを取得
				$('a[href^="#"]').click(function() {
				// 移動先となる要素を取得
				var target = $(this.hash);
				if (!target.length) return ;
					// 移動先となる値
					var targetY = target.offset().top+offsetY;
					// スクロールアニメーション
					$('html,body').animate({scrollTop: targetY}, time, 'swing')
					// ハッシュ書き換えとく
					window.history.pushState(null, null, this.hash);
					// デフォルトの処理はキャンセル
					return false;
					});
					});
			
		block linkfiles



	body
		- var headerlink="index.html"
		block headervar 
		header.header
			a.header-link(href="index.html")
				img.header-link-img(src="/img/white_bulb.png" alt="eeic")
			a(href="index.html").header-top
				.header-top-text
					p.header-top-text EEIC 近未来体験2018
					div.header-top-stripe
			.header-hamburger#navToggle
				span.header-hamburger-bar
				span.header-hamburger-bar
				span.header-hamburger-bar
			nav.header-global-nav
				ul.header-global-nav-list
					li.header-global-nav-list-item.no-wrap
						a.nav-content(href=headerlink+"#contents") 企画一覧
					li.header-global-nav-list-item.no-wrap
						a.nav-content(href=headerlink+"#access") アクセス
					li.header-global-nav-list-item.no-wrap
						a.nav-content(href=headerlink+"#links") リンク
					
		section.under-header#top

		main
			block contents

		footer.footer
			p.footer-copyright Copyright &copy; EEIC 五月祭展示「近未来体験2018」
				br.mobile
				|All rights reserved.
